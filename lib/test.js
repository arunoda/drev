// var redis = require("redis"),
//     client1 = redis.createClient(), client2 = redis.createClient(),
//     msg_count = 0;

//  client2.publish("hello", "I am sending a message.");

function foo() {
    for(var index in argum)
}

foo(10, 20, 20);
